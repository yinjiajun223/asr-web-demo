{
  "name": "tencent-file-asr-flash-demo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client",
    "check": "npm run check -w client && node --check server/src/index.js && node --check server/src/tencentFlash.js && node --test server/src/tencentFlash.test.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
